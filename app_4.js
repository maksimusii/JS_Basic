
'use strict';
// функция сложения
function summ(a, b) {
        return (a + b);
}
// функция вычитания
function sub(a, b) {
    return (a - b);
}
// функция деления
function div(a, b) {
    return (a / b);
}
// функция деления
function mul(a, b) {
    return (a * b);
}

console.log(sub(5, 7));
